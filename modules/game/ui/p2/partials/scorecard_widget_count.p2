<div class="flex-container flex-row flex-justify">
  <label for="{{ field_id }}">
    {{ state.Name }}{% if state.Desc %} (<em>{{ state.Desc }}</em>){% endif %}
  </label>
  <input type="number"
         style="width: 4em;"
         id="{{ field_id }}"
         name="{{ field_id }}"
         min="0"
         {% if state.Max>0 %}max="{{ state.Max }}"{% endif %}
         value="{{ ScorecardData|key:field_id }}"
         {% if edit_disable %}disabled{% endif %}
         />
</div>
